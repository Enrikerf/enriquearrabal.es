version: '3.7'

services:

  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    volumes:
      - './app:/app'
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true